/* The Hardin Civic — site.js
 *
 * v1: intentionally empty. The page works with zero JavaScript.
 * Next step adds the AI streaming client here (fetch + ReadableStream).
 */
(() => {
    'use strict';
    // Future: bind the .ask form, stream xAI responses, etc.
})();
